export * from './BrandLogo';
// AnimatedShaderBackground: lazy-load directly if needed (pulls in three.js)
export * from './rainbow-button';
// sparkles is lazy-loaded directly by consumers (tsparticles is large)
